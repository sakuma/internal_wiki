= form_for @wiki_information do |f|
  - if @wiki_information.errors.any?
    #error_explanation
      %h2= "#{pluralize(@wiki_information.errors.count, "error")} prohibited this wiki_information from being saved:"
      %ul
        - @wiki_information.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :is_private
    = f.check_box :is_private
  .field
    = f.label :created_by
    = f.number_field :created_by
  .actions
    = f.submit 'Save'
