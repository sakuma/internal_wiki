- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div{:class => "alert #{alert_css_class(name)} alert-dismissable"}
      %a.close{"data-dismiss" => "alert"} &times;
      = content_tag :div, raw(msg), id: "flash_#{name}", class: 'text-center'
